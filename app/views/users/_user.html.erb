<tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <!-- <td><%= link_to 'Show', user %></td> -->
    <td><% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "Are you sure wanna delete this User?" } %></td>
  <% end %>
</tr>
